sap.ui.define([
    "sap/ui/core/mvc/Controller"
], (Controller) => {
    "use strict";

    return Controller.extend("sync.d04.exercised0406.controller.View", {
        onInit() {
        },
        // //  성별에 따른 아이콘 반환 함수
        // formatGenderIcon(sGender) {
        //     return sGender === "남" ? "sap-icon://male" : "sap-icon://female";
        // }
    });
});